#!/usr/bin/env bash
# Legacy no-op — Emacs is now launched directly at Hyprland startup (userprefs.conf exec-once).
# The Emacs server starts automatically via (server-start) in tools.el.
# Kept for backward compatibility with any scripts that still source or call it.
exit 0
