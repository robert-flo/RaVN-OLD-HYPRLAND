#!/usr/bin/env bash
# Open a new Emacs GUI frame (Super+Shift+E).
# Emacs is launched at Hyprland startup (userprefs.conf exec-once), so server is always available.
set -euo pipefail

export GDK_BACKEND="${GDK_BACKEND:-wayland}"
export TERM="${TERM:-xterm-256color}"
export COLORTERM="${COLORTERM:-truecolor}"

exec emacsclient -c -n -e "(delete-other-windows)"
